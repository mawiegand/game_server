module GameRules::RulesHelper
  
  # def building_type_with_symbolic_id(symbolic_id)
    # GameRules::Rules.the_rules.building_types.each do |type|
      # return type if type[:symbolic_id].to_s == symbolic_id.to_s
    # end
    # return nil      
  # end
#   
  # def building_type_with_id(id)
    # building_type = GameRules::Rules.the_rules.building_types[id]
    # raise InternalServerError.new("Could not find building id #{id} in rules") if building_type.nil?
    # return building_type      
  # end
  
end
