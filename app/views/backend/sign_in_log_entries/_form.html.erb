<%= form_for(@backend_sign_in_log_entry) do |f| %>
  <% if @backend_sign_in_log_entry.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@backend_sign_in_log_entry.errors.count, "error") %> prohibited this backend_sign_in_log_entry from being saved:</h2>

      <ul>
      <% @backend_sign_in_log_entry.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :character_id %><br />
    <%= f.number_field :character_id %>
  </div>
  <div class="field">
    <%= f.label :user_agent %><br />
    <%= f.text_area :user_agent %>
  </div>
  <div class="field">
    <%= f.label :referer %><br />
    <%= f.text_field :referer %>
  </div>
  <div class="field">
    <%= f.label :referer_url %><br />
    <%= f.text_area :referer_url %>
  </div>
  <div class="field">
    <%= f.label :remote_ip %><br />
    <%= f.text_field :remote_ip %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
