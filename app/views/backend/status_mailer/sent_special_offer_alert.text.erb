Das Update-Skript hat eine Buchung gefunden, die ein Premiumpaket enthält:

  Buchungsdatum:   <%= DateTime.parse(@transaction.updatetstamp).to_time.to_s(:human) %>
  Betrag:          <%= @transaction.gross %>
  Identifier:      <%= @transaction.partner_user_id %> <%= GAME_SERVER_CONFIG['identity_provider_base_url'] + "/identities/#{@transaction.partner_user_id}" %>
  Name:            <%= @transaction.payer_first_name.blank? ? "" : @transaction.payer_first_name + " "%><%= @transaction.payer_last_name %>
  E-Mailadresse:   <%= @transaction.payer_mail %>
  Invoice ID:      <%= @transaction.invoice_id %>

  Alle Buchungen:  <%= GAME_SERVER_CONFIG['base_url'] + "/game_server/shop/money_transactions" %>

Whack On!

- cron