<%= form_for(@backend_tutorial_stat) do |f| %>
  <% if @backend_tutorial_stat.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@backend_tutorial_stat.errors.count, "error") %> prohibited this backend_tutorial_stat from being saved:</h2>

      <ul>
      <% @backend_tutorial_stat.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :cohort_size %><br />
    <%= f.number_field :cohort_size %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
