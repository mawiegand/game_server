<%= form_for(@facebook_app_config) do |f| %>
  <% if @facebook_app_config.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@facebook_app_config.errors.count, "error") %> prohibited this facebook_app_config from being saved:</h2>

      <ul>
      <% @facebook_app_config.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :app_id %><br />
    <%= f.text_field :app_id %>
  </div>
  <div class="field">
    <%= f.label :app_secret %><br />
    <%= f.text_field :app_secret %>
  </div>
  <div class="field">
    <%= f.label :app_token %><br />
    <%= f.text_field :app_token %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
