<%= form_for(@fundamental_artifact_initiation) do |f| %>
  <% if @fundamental_artifact_initiation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@fundamental_artifact_initiation.errors.count, "error") %> prohibited this fundamental_artifact_initiation from being saved:</h2>

      <ul>
      <% @fundamental_artifact_initiation.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :artifact_id %><br />
    <%= f.number_field :artifact_id %>
  </div>
  <div class="field">
    <%= f.label :started_at %><br />
    <%= f.datetime_select :started_at %>
  </div>
  <div class="field">
    <%= f.label :finished_at %><br />
    <%= f.datetime_select :finished_at %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
