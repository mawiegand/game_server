<!DOCTYPE html>
<html>
<head>
  <title>5d Lab | <%= @title %></title>
  <%= stylesheet_link_tag    "application" %>
  <%= csrf_meta_tags %>
</head>
<body>
  
  <% flash.each do |key, value| %>
    <div class="flash <%= key %>"><%= value %></div>
  <% end %>
  <%= content_for?(:content) ? yield(:content) : yield %>

</body>
</html>
