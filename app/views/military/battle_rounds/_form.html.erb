<%= form_for(@military_battle_round) do |f| %>
  <% if @military_battle_round.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@military_battle_round.errors.count, "error") %> prohibited this military_battle_round from being saved:</h2>

      <ul>
      <% @military_battle_round.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :battle_id %><br />
    <%= f.number_field :battle_id %>
  </div>
  <div class="field">
    <%= f.label :round_num %><br />
    <%= f.number_field :round_num %>
  </div>
  <div class="field">
    <%= f.label :executed_at %><br />
    <%= f.datetime_select :executed_at %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
