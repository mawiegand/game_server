<%= form_for(@shop_special_offers_transaction) do |f| %>
  <% if @shop_special_offers_transaction.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@shop_special_offers_transaction.errors.count, "error") %> prohibited this shop_special_offers_transaction from being saved:</h2>

      <ul>
      <% @shop_special_offers_transaction.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :character_id %><br />
    <%= f.number_field :character_id %>
  </div>
  <div class="field">
    <%= f.label :offer_id %><br />
    <%= f.number_field :offer_id %>
  </div>
  <div class="field">
    <%= f.label :state %><br />
    <%= f.number_field :state %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
