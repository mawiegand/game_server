<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE Rules SYSTEM "../rules.dtd">

<Building id="building_altar" advisor="chef" group="miscellaneous" category="building_category_special_building" buyable="false" destructable="false" demolishable="true" experienceFactor="8" takeoverDestroy="true">
  <Name lang="de_DE">Ritualstein</Name>
  <Name lang="en_US">Altar</Name>
  <Description lang="de_DE">
    <p>Spezialisierungsgebäude, spezalisiert die Lagerstätte, so dass sie nicht mehr übernommen werden kann. Nur ein Spezialisierungsgebäude pro Lagerstätte. Der Ritualstein schützt eine Lagerstätte vor der Übernahme durch gegnerische Spieler.</p>
   </Description>
  <Description lang="en_US">
    <p>Specialization building, specializes your camp, making it impossible to be overtaken. Only one specialization building per camp is possible. An Altar makes it impossible for your enemies to take over your camp.</p>
  </Description>
  <Flavour lang="en_US">
    <p></p>
  </Flavour>
    <Flavour lang="de_DE">
    <p></p>
   </Flavour> 
 <Population>FLOOR(((MIN(LEVEL+1,7)-MIN(LEVEL,7))*(1.7*POW(LEVEL,1.65))+(MIN(LEVEL,7)-MIN(LEVEL,6))*(3.75*POW((LEVEL-6),2)+14.75*(LEVEL-6)+31.25)+(MIN(LEVEL,11)-MIN(LEVEL,10))*25+EQUAL(LEVEL,20)*2+0.5)*1.5)</Population>  
  <RequirementGroup>
    <Requirement id="building_haunt" min_level="6" />
    <Requirement id="building_chief_cottage" max_level="0" />
    <Requirement id="building_field_camp" max_level="0" />
  </RequirementGroup>
  <Cost id="resource_stone"  round="true">FLOOR(((0.9*POW(MIN(LEVEL,10),4)-9.7*POW(MIN(LEVEL,10),3)+49.25*POW(MIN(LEVEL,10),2)-76*MIN(LEVEL,10)+70)*((MIN(LEVEL+1,11)-MIN(LEVEL,11))*0.02+(0.06*(MAX(LEVEL-10,0))+0.98)))*3*1.5+0.5)</Cost>
  <Cost id="resource_wood"  round="true">FLOOR(((0.9*POW(MIN(LEVEL,10),4)-9.7*POW(MIN(LEVEL,10),3)+49.25*POW(MIN(LEVEL,10),2)-76*MIN(LEVEL,10)+70)*((MIN(LEVEL+1,11)-MIN(LEVEL,11))*0.02+(0.06*(MAX(LEVEL-10,0))+0.98)))*3*1.5+0.5)</Cost>
  <Cost id="resource_fur"  round="true">FLOOR(((0.9*POW(MIN(LEVEL,10),4)-9.7*POW(MIN(LEVEL,10),3)+49.25*POW(MIN(LEVEL,10),2)-76*MIN(LEVEL,10)+70)*((MIN(LEVEL+1,11)-MIN(LEVEL,11))*0.02+(0.06*(MAX(LEVEL-10,0))+0.98)))*2*1.5+0.5)</Cost>
	  <ProductionTime shortenable="true">EQUAL(LEVEL,1)*36*3600+GREATER(LEVEL,1)*(3600*LEVEL)</ProductionTime>
  <Abilities> <!-- just for grouping -->
    <PreventTakeover level="1" />
  </Abilities>
</Building>
