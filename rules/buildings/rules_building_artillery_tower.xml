<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE Rules SYSTEM "../rules.dtd">

<Building id="building_artillery_tower" group="military" category="building_category_fortress_tower" buyable="true" destructable="true" demolishable="true">  
  <Name lang="de_DE">Turm der Ballistik</Name>
  <Name lang="en_US">Ballistic Tower</Name>
  <Description lang="de_DE">
    <p>Von außen betrachtet ist der Turm der Ballistik eine Augenweide. Von innen betrachtet ist der Turm ein Trümmerfeld. Kleine Kieselsteine liegen auf dem ganzen Boden verteilt, Bratspieße, Speere und Pfeile haben sich in sämtliche Stützpfeiler gebohrt und an den Wänden spuren hinterlassen.</p>
    <p>Nicht weiter verwunderlich ist da die Helmpflicht. Die Ausbilder und Auszubildenden haben sich feste Tierhäute um den Kopf gebunden, damit sie den Aufprall kleiner Steiner oder Splitter halbwegs überstehen.</p>
    <p>Verglichen mit der Ordnung einer Fernkämpfer-Kampfreihe auf dem Schlachtfeld ist das herrschende Chaos überraschend. Auf ein Kommando werden alle Arten von Wurfgeschossen in die Luft gesandt. Nur leider wissen die Wenigsten, auf welches Kommando sie eigentlich gerade achten sollen.</p>
	</Description>
  <Description lang="en_US">
    <p>Trains artillery.</p>
  </Description>
  <Flavour lang="en_US">
    <p>Everything which can be shoot flies around here. So, watch yourself and duck in time! Training of long-range combat fighter is in progress.</p>
  </Flavour>
    <Flavour lang="de_DE">
    <p>Hier fliegt alles, was man werfen oder abschießen kann! Kopf runter! Die Ausbildung der Fernkämpfer ist in vollem Gang.</p>
  </Flavour>
  <Population>2.5*POW(LEVEL,2)-2.5*LEVEL+5</Population>
  <RequirementGroup>
    <Requirement id="building_fortress_fortification" min_level="8" />  
  </RequirementGroup>
  <Cost id="resource_stone" round="true">FLOOR(((0.9*POW(MIN(LEVEL+6,10),4)-9.7*POW(MIN(LEVEL+6,10),3)+49.25*POW(MIN(LEVEL+6,10),2)-76*MIN(LEVEL+6,10)+70)*((MIN(LEVEL+7,11)-MIN(LEVEL+6,11))*0.02+(0.06*(MAX(LEVEL-4,0))+0.98)))*1.5+0.5)</Cost>  
  <Cost id="resource_wood" round="true">FLOOR(((0.9*POW(MIN(LEVEL+6,10),4)-9.7*POW(MIN(LEVEL+6,10),3)+49.25*POW(MIN(LEVEL+6,10),2)-76*MIN(LEVEL+6,10)+70)*((MIN(LEVEL+7,11)-MIN(LEVEL+6,11))*0.02+(0.06*(MAX(LEVEL-4,0))+0.98)))*3+0.5)</Cost>    
  <Cost id="resource_fur" round="true">(MIN(LEVEL,3)-MIN(LEVEL,2))*FLOOR(((0.9*POW(MIN(LEVEL+6,10),4)-9.7*POW(MIN(LEVEL+6,10),3)+49.25*POW(MIN(LEVEL+6,10),2)-76*MIN(LEVEL+6,10)+70)*((MIN(LEVEL+7,11)-MIN(LEVEL+6,11))*0.02+(0.06*(MAX(LEVEL-4,0))+0.98)))*0.5+0.5)</Cost>
  <Cost id="resource_cash" round="true">MAX(LEVEL-14,0)</Cost> 
  <ProductionTime shortenable="true">43200+(FLOOR(1.88*POW(LEVEL-1,1.8)+0.5)*6/100.0+1)*9000</ProductionTime>
  <Abilities> <!-- just for grouping -->
    <UnlockQueue  queue="queue_artillery" />
    <SpeedupQueue queue="queue_artillery" domain="settlement">FLOOR(1.88*POW(LEVEL,1.425)*0.5+0.5)/100.0</SpeedupQueue>
  </Abilities>  
</Building>
