<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE Rules SYSTEM "../rules.dtd">

<Building id="building_barracks" group="military" category="building_category_small_building" buyable="true" destructable="true" demolishable="true" experienceFactor="4.5">  
  <Name lang="de_DE">Ausbildungsgelände</Name>
  <Name lang="en_US">Training Grounds</Name>
  <Description lang="de_DE">
    <p>Auf dem Ausbildungsgelände werden alle Arten von Nahkämpfern ausgebildet. Große Keule, Bratspieß oder doch die bloßen Fäuste, alles ist erlaubt.</p>
	<p>In zahlreichen Wettbewerben messen sich die kommenden Krieger, um sich im Zweikampf zu stählen. Einmal im Mondumlauf wird ein öffentliches Turnier veranstaltet. Der Sieger bekommt alles. Ruhm, Essen, einen Tag frei und Männer soviel sie wollen. Ja Männer! Denn zumeist gewinnt eine Frau dieses Turnier. Wie? Mit den tödlichen Waffen einer Frau natürlich!</p>
  </Description>
  <Description lang="en_US">
    <p>These are the training grounds where all kinds of close combat fighters are trained. Big clubs, roasting spits, or bare fists – anything goes.</p>
    <p>Would-be combatants compete in numerous contests to toughen themselves up for duelling. Once in a moon a public tournament is held. The winner gets the lot: Glory, food, a day off and as many men as they want. Yes, that’s right: men! Because the tournaments are usually won by women. How? With a woman’s deadliest weapons, of course!</p>
  </Description>
  <Flavour lang="en_US">
    <p>Looking for a little fight? Haven’t been bashed up for a while? The barracks are the place to go for anyone wanting to jump into the fray.</p>
  </Flavour>
    <Flavour lang="de_DE">
    <p>Auf der Suche nach einem kleinen Kampf? Lange nicht mehr verprügelt worden?</p>
  </Flavour>
<Population>FLOOR(((MIN(LEVEL+1,7)-MIN(LEVEL,7))*(1.7*POW(LEVEL,1.65))+(MIN(LEVEL,7)-MIN(LEVEL,6))*(3.75*POW((LEVEL-6),2)+14.75*(LEVEL-6)+31.25)+(MIN(LEVEL,11)-MIN(LEVEL,10))*25+EQUAL(LEVEL,20)*2+0.5)*2)</Population>
  <RequirementGroup>
    <Requirement id="building_chief_cottage" min_level="2" />
    <Requirement id="building_haunt" max_level="0" />
  </RequirementGroup>
  <RequirementGroup>
    <Requirement id="building_haunt" min_level="2" />
    <Requirement id="building_chief_cottage" max_level="0" />
  </RequirementGroup>
  <Cost id="resource_stone" round="true">EQUAL(LEVEL,1)*50+GREATER(LEVEL,1)*FLOOR(((0.9*POW(MIN(LEVEL,10),4)-9.7*POW(MIN(LEVEL,10),3)+49.25*POW(MIN(LEVEL,10),2)-76*MIN(LEVEL,10)+70)*((MIN(LEVEL+1,11)-MIN(LEVEL,11))*0.02+(0.06*(MAX(LEVEL-10,0))+0.98)))*3*1.5+0.5)</Cost>
  <Cost id="resource_wood"  round="true">EQUAL(LEVEL,1)*25+GREATER(LEVEL,1)*FLOOR(((0.9*POW(MIN(LEVEL,10),4)-9.7*POW(MIN(LEVEL,10),3)+49.25*POW(MIN(LEVEL,10),2)-76*MIN(LEVEL,10)+70)*((MIN(LEVEL+1,11)-MIN(LEVEL,11))*0.02+(0.06*(MAX(LEVEL-10,0))+0.98)))*1.5*1.5+0.5)</Cost>
    <Cost id="resource_cash" round="true">2*MAX(LEVEL-19,0)</Cost>
  <ProductionTime shortenable="true">EQUAL(LEVEL,1)*15+GREATER(LEVEL,1)*FLOOR(((MIN(LEVEL+1,4)-MIN(LEVEL,4))*(130*POW(LEVEL,2)-350*LEVEL+240)+GREATER(LEVEL,3)*(MIN(LEVEL+1,11)-MIN(LEVEL,11))*30*POW(LEVEL,3.2)+GREATER(LEVEL,10)*47547*(0.06*(LEVEL-10)+0.98))*3+0.5)</ProductionTime>
  <Abilities> <!-- just for grouping -->
    <UnlockQueue  queue="queue_infantry" />
    <SpeedupQueue queue="queue_infantry" domain="settlement">LESS(LEVEL,11)*FLOOR(1.25*POW(LEVEL,1.3)+0.5)/100.0+GREATER(LEVEL,10)*FLOOR(0.3*POW(LEVEL,1.94)+0.5)/100.0</SpeedupQueue>
  </Abilities>
</Building>
