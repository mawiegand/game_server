<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE Rules SYSTEM "../rules.dtd">

<Building id="building_command_post" group="military" category="building_category_large_building" buyable="true" destructable="false" demolishable="false" tier="2">  
  <Name lang="de_DE">Kommandozentrale</Name>
  <Name lang="en_US">Command Post</Name>
  <Description lang="de_DE">
    <p>Der Kommandoposten erhöht die maximale Anzahl an Armeen und beschleunigt die Einheitenrekrutierung.</p> 
  </Description>
  <Description lang="en_US">
    <p>At start it is more a hut or but later an area to represent the chieftain‘s glory, advancement and power.</p> 
	<p>Symbols of triumph, the banners and iconic loot is shown off here. Rumor has it that the chieftain uses his hall for excessive orgies from time to time!</p>
  </Description>
  <Flavour lang="en_US">
    <p> lustiger Flavour Text hier </p>
  </Flavour>
    <Flavour lang="de_DE">
    <p>Ein Kommandoposten koordiniert die verschiedenen Armeen im Felde. Soweit man mehr Armeen in eine Richtung schicken als koordinieren bezeichnen kann.</p>  
	</Flavour> 
  <Cost id="resource_stone" round="true">FLOOR(((0.9*POW(MIN(LEVEL,10),4)-9.7*POW(MIN(LEVEL,10),3)+49.25*POW(MIN(LEVEL,10),2)-76*MIN(LEVEL,10)+70)*((MIN(LEVEL+1,11)-MIN(LEVEL,11))*0.02+(0.06*(MAX(LEVEL-10,0))+0.98)))*1.5+0.5)</Cost>  
  <Cost id="resource_wood" round="true">FLOOR(((0.9*POW(MIN(LEVEL,10),4)-9.7*POW(MIN(LEVEL,10),3)+49.25*POW(MIN(LEVEL,10),2)-76*MIN(LEVEL,10)+70)*((MIN(LEVEL+1,11)-MIN(LEVEL,11))*0.02+(0.06*(MAX(LEVEL-10,0))+0.98)))*1.5+0.5)</Cost>
  <Requirement id="building_copper_smelter" min_level="5" />
  <Requirement id="building_command_post" max_level="0" />
  <ProductionTime shortenable="false">FLOOR(((MIN(LEVEL+1,4)-MIN(LEVEL,4))*(130*POW(LEVEL,2)-350*LEVEL+240)+(MIN(LEVEL,4)-MIN(LEVEL,3))*(MIN(LEVEL+1,11)-MIN(LEVEL,11))*19.5*POW(LEVEL,3.6)/4.3+(MIN(LEVEL,11)-MIN(LEVEL,10))*18054*(0.06*(LEVEL-10)+0.98))*4+0.5)</ProductionTime>  
  <Abilities> <!-- just for grouping -->
    <CommandPoints>1+FLOOR(LEVEL/10.0)</CommandPoints>
    <SpeedupQueue queue="queue_infantry" domain="settlement">MIN(MAX(LEVEL-10,0),1)*FLOOR(1.88*POW(LEVEL,1.425)+0.5)/100.0</SpeedupQueue>
    <SpeedupQueue queue="queue_artillery" domain="settlement">MIN(MAX(LEVEL-10,0),1)*FLOOR(1.88*POW(LEVEL,1.425)+0.5)/100.0</SpeedupQueue>
    <SpeedupQueue queue="queue_cavalry" domain="settlement">MIN(MAX(LEVEL-10,0),1)*FLOOR(1.88*POW(LEVEL,1.425)+0.5)/100.0</SpeedupQueue>
  </Abilities>
</Building>
  
