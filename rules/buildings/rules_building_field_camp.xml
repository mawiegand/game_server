<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE Rules SYSTEM "../rules.dtd">

<Building id="building_field_camp" advisor="warrior" group="military" category="building_category_special_building" buyable="false" destructable="false" demolishable="true"  experienceFactor="8" takeoverDestroy="true">
  <Name lang="de_DE">Feldlager</Name>
  <Name lang="en_US">Field Camp</Name>
  <Description lang="de_DE">
    <p>Spezialisierungsgebäude, spezalisiert die Lagerstätte für militärische Zwecke. Nur ein Spezialisierungsgebäude pro Lagerstätte. Das Feldlager erhöht die Armeegröße und liefert auf Level 10 einen zusätzlichen Kommandopunkt.</p>
  </Description>
  <Description lang="en_US">
    <p>Specialization building, specializes your camp for military purposes. Only one specialization building per camp is possible. Having a field camp means that more fighters can be deployed: 50 per level at Levels 1-10. At Level 10, a field camp also increases a settlement's command points by one.</p>
  </Description>
  <Flavour lang="en_US">
    <p></p>
  </Flavour>
    <Flavour lang="de_DE">
    <p></p>
  </Flavour>
<Population>FLOOR(((MIN(LEVEL+1,7)-MIN(LEVEL,7))*(1.7*POW(LEVEL,1.65))+(MIN(LEVEL,7)-MIN(LEVEL,6))*(3.75*POW((LEVEL-6),2)+14.75*(LEVEL-6)+31.25)+(MIN(LEVEL,11)-MIN(LEVEL,10))*25+EQUAL(LEVEL,20)*2+0.5)*1.5)</Population> 
 <RequirementGroup>
    <Requirement id="building_haunt" min_level="6" />
    <Requirement id="building_chief_cottage" max_level="0" />
    <Requirement id="building_altar" max_level="0" />
  </RequirementGroup>
  <Cost id="resource_stone" round="true">FLOOR(((0.9*POW(MIN(LEVEL,10),4)-9.7*POW(MIN(LEVEL,10),3)+49.25*POW(MIN(LEVEL,10),2)-76*MIN(LEVEL,10)+70)*((MIN(LEVEL+1,11)-MIN(LEVEL,11))*0.02+(0.06*(MAX(LEVEL-10,0))+0.98)))*3*1.5+0.5)</Cost>  
  <Cost id="resource_wood"  round="true">FLOOR(((0.9*POW(MIN(LEVEL,10),4)-9.7*POW(MIN(LEVEL,10),3)+49.25*POW(MIN(LEVEL,10),2)-76*MIN(LEVEL,10)+70)*((MIN(LEVEL+1,11)-MIN(LEVEL,11))*0.02+(0.06*(MAX(LEVEL-10,0))+0.98)))*3*1.5+0.5)</Cost>
  <Cost id="resource_fur"  round="true">FLOOR(((0.9*POW(MIN(LEVEL,10),4)-9.7*POW(MIN(LEVEL,10),3)+49.25*POW(MIN(LEVEL,10),2)-76*MIN(LEVEL,10)+70)*((MIN(LEVEL+1,11)-MIN(LEVEL,11))*0.02+(0.06*(MAX(LEVEL-10,0))+0.98)))*2*1.5+0.5)</Cost>
	  <ProductionTime shortenable="true">3600*LEVEL</ProductionTime>
  <Abilities> <!-- just for grouping -->
    <GarrisonSizeBonus>100+50*LEVEL-GREATER(LEVEL,10)*(LEVEL-10)*30</GarrisonSizeBonus>
    <ArmySizeBonus>50*LEVEL-GREATER(LEVEL,10)*(LEVEL-10)*30</ArmySizeBonus>
    <CommandPoints>GREATER(LEVEL,9)</CommandPoints>
  </Abilities>
</Building>
