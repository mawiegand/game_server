<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE Rules SYSTEM "../rules.dtd">

<Building id="building_furrier" group="resource" category="building_category_small_building" buyable="true" destructable="true" demolishabe="true">  
  <Name lang="de_DE">Kürschner</Name>
  <Name lang="en_US">Peltmonger</Name>
  <Description lang="de_DE">
    <p>Verarbeitet Häute zu Leder und hat manchmal auch ein paar schöne Säbelzahntigerfelle für die Dame von Welt im Angebot.</p>
  </Description>
  <Description lang="en_US">
    <p>Manufacturing hides into leather. Sometimes there are also must-have sabre cat furs as ready-to-wear fashion for the lady or cosy brontosaurus regulation underwear.</p> 
  </Description>
  <Flavour lang="en_US">
	<p>The peltmonger is literally flaying all day long!</p>
  </Flavour>
    <Flavour lang="de_DE">
    <p>Der Kürschner zieht dem Tier das Fell über die Ohren. Hier gibt es schöne Felle und hochwertige Lederwaren, dazu immer mal ein leckeres Tier auf dem Feuer.</p>
    <p>Ab Level 11 bietet der Kürschner einen lokalen Fellonus. Auf Level 11-19 beträgt dieser 1% je Level: 1-9%, bei Level 20 ist der Fellbonus 15%.</p>
  </Flavour>
  <Requirement id="building_chief_cottage" min_level="8" />  
  <Cost id="resource_wood" round="true">FLOOR(((MIN(LEVEL+1,5)-MIN(LEVEL,5))*(40*POW(LEVEL,2)-80*LEVEL+60)+(MIN(LEVEL,5)-MIN(LEVEL,4))*(MIN(LEVEL+1,11)-MIN(LEVEL,11))*(132*POW(LEVEL,2)-543*LEVEL+520)+(MIN(LEVEL,11)-MIN(LEVEL,10))*8032/2.6*POW(2.71828,0.0953*LEVEL))/2+0.5)</Cost>
  <Cost id="resource_stone" round="true">FLOOR(((MIN(LEVEL+1,5)-MIN(LEVEL,5))*(40*POW(LEVEL,2)-80*LEVEL+60)+(MIN(LEVEL,5)-MIN(LEVEL,4))*(MIN(LEVEL+1,11)-MIN(LEVEL,11))*(132*POW(LEVEL,2)-543*LEVEL+520)+(MIN(LEVEL,11)-MIN(LEVEL,10))*8032/2.6*POW(2.71828,0.0953*LEVEL))/2+0.5)</Cost>  
  <Cost id="resource_fur" round="true">FLOOR(((MIN(LEVEL+1,5)-MIN(LEVEL,5))*(40*POW(LEVEL,2)-80*LEVEL+60)+(MIN(LEVEL,5)-MIN(LEVEL,4))*(MIN(LEVEL+1,11)-MIN(LEVEL,11))*(132*POW(LEVEL,2)-543*LEVEL+520)+(MIN(LEVEL,11)-MIN(LEVEL,10))*8032/2.6*POW(2.71828,0.0953*LEVEL))+0.5)</Cost>   
  <Cost id="resource_cash" round="true">MAX(LEVEL-19,0)</Cost> 
  <Production id="resource_fur">FLOOR(7.5/2*POW(LEVEL,1.33)+0.5)</Production>
  <ProductionBonus id="resource_fur">MAX(LEVEL-10,0)*0.01+MAX(0,LEVEL-19)*0.05</ProductionBonus>
  <ProductionTime shortenable="true">FLOOR(((MIN(LEVEL+1,4)-MIN(LEVEL,4))*(130*POW(LEVEL,2)-350*LEVEL+240)+(MIN(LEVEL,4)-MIN(LEVEL,3))*(MIN(LEVEL+1,11)-MIN(LEVEL,11))*19.5*POW(LEVEL,3.6)+(MIN(LEVEL,11)-MIN(LEVEL,10))*950*POW(LEVEL,2))*3+0.5)</ProductionTime>             
  <Abilities> <!-- just for grouping -->
  </Abilities>
</Building>


