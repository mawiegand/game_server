<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE Rules SYSTEM "../rules.dtd">

<Building id="building_garrison" group="military" category="building_category_small_building" buyable="true" destructable="false" demolishable="false" tier="2">  
  <Name lang="de_DE">Garnisonsgebäude</Name>
  <Name lang="en_US">Garrison</Name>
  <Description lang="de_DE">
    <p>Größere Garnisonen führen zu… größeren Garnisonsarmeen wer hätte das gedacht. Auch die Feldarmeen profitieren von der erhöhten Disziplin.</p> 
  </Description>
  <Description lang="en_US">
    <p>Bigger garrisons lead to… bigger garrison armies who would have thought that. They also affect the effective field army size.</p> 
  </Description>
  <Flavour lang="en_US">
    <p>This is THE place for warriors of all kinds to repeatedly bash their heads in.</p>
  </Flavour>
    <Flavour lang="de_DE">
    <p>Krieger aller Art sollten getrennt von der normalen arbeitenden Bevölkerung gehalten werden. In der Garnison können sie sich gegenseitig die Schädel einschlagen und die armen Siedlungsbewohner in Ruhe lassen.</p>  
  </Flavour> 
  <Population>FLOOR((MIN(LEVEL+1,7)-MIN(LEVEL,7))*(1.7*POW(LEVEL,1.65))+(MIN(LEVEL,7)-MIN(LEVEL,6))*(5*POW(LEVEL,2)+3*LEVEL+43.3)+(MIN(LEVEL,11)-MIN(LEVEL,10))*20+0.5)</Population>
  <Requirement id="building_copper_smelter" min_level="8" />
    <Requirement id="building_garrison" max_level="0" />
  <Requirement id="building_haunt" max_level="0" />
  <Cost id="resource_stone" round="true">FLOOR(((0.9*POW(MIN(LEVEL,10),4)-9.7*POW(MIN(LEVEL,10),3)+49.25*POW(MIN(LEVEL,10),2)-76*MIN(LEVEL,10)+70)*((MIN(LEVEL+1,11)-MIN(LEVEL,11))*0.02+(0.06*(MAX(LEVEL-10,0))+0.98)))*1.5+0.5)</Cost>  
  <Cost id="resource_wood" round="true">FLOOR(((0.9*POW(MIN(LEVEL,10),4)-9.7*POW(MIN(LEVEL,10),3)+49.25*POW(MIN(LEVEL,10),2)-76*MIN(LEVEL,10)+70)*((MIN(LEVEL+1,11)-MIN(LEVEL,11))*0.02+(0.06*(MAX(LEVEL-10,0))+0.98)))*1.5+0.5)</Cost> 
  <ProductionTime shortenable="false">FLOOR(((MIN(LEVEL+1,4)-MIN(LEVEL,4))*(130*POW(LEVEL,2)-350*LEVEL+240)+(MIN(LEVEL,4)-MIN(LEVEL,3))*(MIN(LEVEL+1,11)-MIN(LEVEL,11))*19.5*POW(LEVEL,3.6)/4.3+(MIN(LEVEL,11)-MIN(LEVEL,10))*18054*(0.06*(LEVEL-10)+0.98))*4+0.5)</ProductionTime>     
  <Abilities> <!-- just for grouping -->
    <GarrisonSizeBonus>10*LEVEL</GarrisonSizeBonus>
    <ArmySizeBonus>5*LEVEL</ArmySizeBonus>
  </Abilities> 
</Building>
