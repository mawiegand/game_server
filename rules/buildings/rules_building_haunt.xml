<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE Rules SYSTEM "../rules.dtd">

<Building id="building_haunt" group="miscellaneous" category="building_category_large_building" buyable="true" destructable="false" demolishable="false">  
  <Name lang="de_DE">Versammlungsplatz</Name>
  <Name lang="en_US">Haunt</Name>
  <Description lang="de_DE">
    <p>In der Mitte einer Lagerstätte liegt der Versammlungsplatz. Ein zufällig nicht bebauter Platz für die Ablage von ein paar Rohstoffen und die Zusammenkunft der Bewohner.</p>
  </Description>
  <Description lang="en_US">
    <p>At start it is more a hut or but later an area to represent the chieftain‘s glory, advancement and power.</p> 
	<p>Symbols of triumph, the banners and iconic loot is shown off here. Rumor has it that the chieftain uses his hall for excessive orgies from time to time!</p>
  </Description>
  <Flavour lang="en_US">
    <p> lustiger Flavour Text hier </p>
  </Flavour>
    <Flavour lang="de_DE">
    <p>Hier treffen sich alle wichtigen Personen der Lagerstätte jeden Abend, um die wichtigen Probleme zu besprechen. Zum Beispiel, wie man die Bierflaute lösen kann.</p>  
	</Flavour> 
  <Population>FLOOR((MIN(LEVEL+1,8)-MIN(LEVEL,8))*(0.6245*POW(LEVEL,2.2))+(MIN(LEVEL,8)-MIN(LEVEL,7))*(5*POW((LEVEL-7),2)+10*(LEVEL-7)+50)+(MIN(LEVEL,11)-MIN(LEVEL,10))*17+0.5)</Population>
  <Cost id="resource_stone" round="true">FLOOR(((0.9*POW(MIN(LEVEL,10),4)-9.7*POW(MIN(LEVEL,10),3)+49.25*POW(MIN(LEVEL,10),2)-76*MIN(LEVEL,10)+70)*((MIN(LEVEL+1,11)-MIN(LEVEL,11))*0.02+(0.06*(MAX(LEVEL-10,0))+0.98)))*2+0.5)</Cost>  
  <Cost id="resource_wood" round="true">FLOOR(((0.9*POW(MIN(LEVEL,10),4)-9.7*POW(MIN(LEVEL,10),3)+49.25*POW(MIN(LEVEL,10),2)-76*MIN(LEVEL,10)+70)*((MIN(LEVEL+1,11)-MIN(LEVEL,11))*0.02+(0.06*(MAX(LEVEL-10,0))+0.98)))*2+0.5)</Cost> 
  <Cost id="resource_fur" round="true">FLOOR(((0.9*POW(MIN(LEVEL,10),4)-9.7*POW(MIN(LEVEL,10),3)+49.25*POW(MIN(LEVEL,10),2)-76*MIN(LEVEL,10)+70)*((MIN(LEVEL+1,11)-MIN(LEVEL,11))*0.02+(0.06*(MAX(LEVEL-10,0))+0.98)))*1+0.5)</Cost> 
  <Requirement id="building_haunt" max_level="0" />
  <Requirement id="building_chief_cottage" max_level="0" />  
  <ProductionTime shortenable="false">FLOOR(((MIN(LEVEL+1,4)-MIN(LEVEL,4))*(130*POW(LEVEL,2)-350*LEVEL+240)+(MIN(LEVEL,4)-MIN(LEVEL,3))*(MIN(LEVEL+1,11)-MIN(LEVEL,11))*19.5*POW(LEVEL,3.6)/4.3+(MIN(LEVEL,11)-MIN(LEVEL,10))*18054*(0.06*(LEVEL-10)+0.98))*4+0.5)</ProductionTime>     
  <Abilities> <!-- just for grouping -->
    <CommandPoints>1</CommandPoints>
    <GarrisonSizeBonus>200</GarrisonSizeBonus>
    <ArmySizeBonus>200</ArmySizeBonus>
    <UnlockQueue  queue="queue_buildings" />
    <UnlockGarrison level="2" />
    <DefenseBonus>0.25*LEVEL</DefenseBonus>
    <UnlockBuildingSlots>4</UnlockBuildingSlots>
  </Abilities>
</Building>
  
