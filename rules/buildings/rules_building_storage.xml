<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE Rules SYSTEM "../rules.dtd">

<Building id="building_storage" group="miscellaneous" category="building_category_small_building" buyable="true" destructable="true" demolishable="true" experienceFactor="1.5">  
  <Name lang="de_DE">Rohstofflager</Name>
  <Name lang="en_US">Storage</Name>
  <Description lang="de_DE">
        <p>Steinzeitliches Logistikzentrum zum Lagern und Versenden von Rohstoffen. Je größer das Lager, desto mehr Karren können versendet werden.</p>
   </Description>
  <Description lang="en_US">
    <p>Storage space.</p>
  </Description>
  <Flavour lang="en_US">
    <p>Stores your resources.</p>
  </Flavour>
  <Flavour lang="de_DE">
    <p>Steincontainer, Saurierkräne und seit neuestem, Karren. Soviel Stil muss sein!</p>
  </Flavour> 
  <Population>FLOOR((MIN(LEVEL+1,8)-MIN(LEVEL,8))*(0.6245*POW(LEVEL,2.2))+(MIN(LEVEL,8)-MIN(LEVEL,7))*(5*POW((LEVEL-7),2)+10*(LEVEL-7)+50)+(MIN(LEVEL,11)-MIN(LEVEL,10))*17+0.5)</Population>
  <RequirementGroup>
    <Requirement id="building_chief_cottage" min_level="4" />
    <Requirement id="building_haunt" max_level="0" />
  </RequirementGroup>
  <RequirementGroup>
    <Requirement id="building_haunt" min_level="4" />
    <Requirement id="building_chief_cottage" max_level="0" />
  </RequirementGroup>
  <Cost id="resource_stone" round="true">FLOOR(((0.9*POW(MIN(LEVEL,10),4)-9.7*POW(MIN(LEVEL,10),3)+49.25*POW(MIN(LEVEL,10),2)-76*MIN(LEVEL,10)+70)*((MIN(LEVEL+1,11)-MIN(LEVEL,11))*0.02+(0.06*(MAX(LEVEL-10,0))+0.98)))*2+0.5)</Cost>  
  <Cost id="resource_wood" round="true">FLOOR(((0.9*POW(MIN(LEVEL,10),4)-9.7*POW(MIN(LEVEL,10),3)+49.25*POW(MIN(LEVEL,10),2)-76*MIN(LEVEL,10)+70)*((MIN(LEVEL+1,11)-MIN(LEVEL,11))*0.02+(0.06*(MAX(LEVEL-10,0))+0.98)))*1*1.5+0.5)</Cost>
    <Cost id="resource_cash" round="true">MAX(LEVEL-19,0)</Cost> 
<ProductionTime shortenable="true">FLOOR(((MIN(LEVEL+1,4)-MIN(LEVEL,4))*(130*POW(LEVEL,2)-350*LEVEL+240)+(MIN(LEVEL,4)-MIN(LEVEL,3))*(MIN(LEVEL+1,11)-MIN(LEVEL,11))*30*POW(LEVEL,3.2)+(MIN(LEVEL,11)-MIN(LEVEL,10))*47547*(0.06*(LEVEL-10)+0.98))*1.1*1.5+0.5)</ProductionTime>         
  <Capacity id="resource_stone">FLOOR(((MIN(LEVEL, 1)-MIN(LEVEL-1,1))*20 + POW(LEVEL,2) * 80)*1)</Capacity>
  <Capacity id="resource_wood">FLOOR(((MIN(LEVEL, 1)-MIN(LEVEL-1,1))*20 + POW(LEVEL,2) * 80)*1)</Capacity>
  <Capacity id="resource_fur">FLOOR(((MIN(LEVEL, 1)-MIN(LEVEL-1,1))*20 + POW(LEVEL,2) * 80)*0.5)</Capacity>
  <Abilities> <!-- just for grouping -->
    <TradingCarts>10*LEVEL*LEVEL</TradingCarts>
    <UnlockP2PTrade level="1" />
  </Abilities>
  <ConversionOption building="building_storage_2" level="LEVEL-(MIN(LEVEL,5)-MIN(LEVEL,4))-(MIN(LEVEL,10)-MIN(LEVEL,9))-(MIN(LEVEL,14)-MIN(LEVEL,13))"/>  
</Building>

